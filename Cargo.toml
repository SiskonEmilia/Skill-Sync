[package]
name = "skill-sync"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "sync"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }

[target.'cfg(windows)'.dependencies]
junction = "2"

[dev-dependencies]
tempfile = "3"
